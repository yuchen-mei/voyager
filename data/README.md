# Data
This directory contains various datasets that are used to verify the accelerator. 

- `imagenet/` Subset of the ImageNet dataset (ResNet).
- `imagenette/` Contains a small sample of images taken from the [Imagenette](https://github.com/fastai/imagenette) dataset used to generate the data and verify the ResNet model.
- `imdb/` TODO (MobileBERT)
- `sst2_eval/` TODO (MobileBERT)
- `sst2_train/` TODO (MobileBERT)